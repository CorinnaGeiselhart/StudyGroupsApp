<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundcolor">@color/windowBackground</item>
        <item name="textcolor">@color/textColorPrimary</item>
        <item name="hintcolor">@color/textColorHint</item>
        <item name="tintcolor">@color/textColorPrimary</item>
        <item name="buttoncolor">@color/buttonColor</item>
        <item name="navigationbarcolor">@color/navigationBarColor</item>
        <item name="headercolor">@color/colorHeader</item>
        <item name="primarycolor">@color/colorPrimary</item>
    </style>

    <!-- Dark application theme. -->
    <style name="DarkModeTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textColorPrimaryDarkmode</item>
        <item name="android:buttonBarStyle">@color/textColorPrimaryDarkmode</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundcolor">@color/windowBackgroundDarkmode</item>
        <item name="textcolor">@color/textColorPrimaryDarkmode</item>
        <item name="hintcolor">@color/textColorHintDarkmode</item>
        <item name="tintcolor">@color/textColorPrimaryDarkmode</item>
        <item name="buttoncolor">@color/buttonColorDarkmode</item>
        <item name="navigationbarcolor">@color/navigationBarColorDarkmode</item>
        <item name="headercolor">@color/colorHeaderDarkmode</item>
        <item name="primarycolor">@color/colorPrimary</item>
    </style>


    <!-- Ice application theme. -->
    <style name="IceTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="colorPrimary">@color/colorIce</item>
        <item name="colorPrimaryDark">@color/colorIceHeader</item>
        <item name="colorAccent">@color/colorIce</item>
        <item name="backgroundcolor">@color/windowBackground</item>
        <item name="textcolor">@color/textColorPrimary</item>
        <item name="hintcolor">@color/textColorHint</item>
        <item name="tintcolor">@color/textColorPrimary</item>
        <item name="buttoncolor">@color/buttonColor</item>
        <item name="navigationbarcolor">@color/navigationBarColor</item>
        <item name="headercolor">@color/colorIce</item>
        <item name="primarycolor">@color/colorIce</item>
    </style>

    <!-- Ice Dark application theme. -->
    <style name="IceDarkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textColorPrimaryDarkmode</item>
        <item name="android:buttonBarStyle">@color/textColorPrimaryDarkmode</item>
        <item name="colorPrimary">@color/colorIce</item>
        <item name="colorPrimaryDark">@color/colorIceHeader</item>
        <item name="colorAccent">@color/colorIce</item>
        <item name="backgroundcolor">@color/windowBackgroundDarkmode</item>
        <item name="textcolor">@color/textColorPrimaryDarkmode</item>
        <item name="hintcolor">@color/textColorHintDarkmode</item>
        <item name="tintcolor">@color/textColorPrimaryDarkmode</item>
        <item name="buttoncolor">@color/buttonColorDarkmode</item>
        <item name="navigationbarcolor">@color/navigationBarColorDarkmode</item>
        <item name="headercolor">@color/colorIceDark</item>
        <item name="primarycolor">@color/colorIce</item>
    </style>

    <!-- Sun application theme. -->
    <style name="SunTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="colorPrimary">@color/colorSun</item>
        <item name="colorPrimaryDark">@color/colorSunHeader</item>
        <item name="colorAccent">@color/colorSun</item>
        <item name="backgroundcolor">@color/windowBackground</item>
        <item name="textcolor">@color/textColorPrimary</item>
        <item name="hintcolor">@color/textColorHint</item>
        <item name="tintcolor">@color/textColorPrimary</item>
        <item name="buttoncolor">@color/buttonColor</item>
        <item name="navigationbarcolor">@color/navigationBarColor</item>
        <item name="headercolor">@color/colorSun</item>
        <item name="primarycolor">@color/colorSun</item>
    </style>

    <!-- Sun Dark application theme. -->
    <style name="SunDarkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textColorPrimaryDarkmode</item>
        <item name="android:buttonBarStyle">@color/textColorPrimaryDarkmode</item>
        <item name="colorPrimary">@color/colorSun</item>
        <item name="colorPrimaryDark">@color/colorSunHeader</item>
        <item name="colorAccent">@color/colorSun</item>
        <item name="backgroundcolor">@color/windowBackgroundDarkmode</item>
        <item name="textcolor">@color/textColorPrimaryDarkmode</item>
        <item name="hintcolor">@color/textColorHintDarkmode</item>
        <item name="tintcolor">@color/textColorPrimaryDarkmode</item>
        <item name="buttoncolor">@color/buttonColorDarkmode</item>
        <item name="navigationbarcolor">@color/navigationBarColorDarkmode</item>
        <item name="headercolor">@color/colorSunDark</item>
        <item name="primarycolor">@color/colorSun</item>
    </style>

    <!-- Nature application theme. -->
    <style name="NatureTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="colorPrimary">@color/colorNature</item>
        <item name="colorPrimaryDark">@color/colorNatureHeader</item>
        <item name="colorAccent">@color/colorNature</item>
        <item name="backgroundcolor">@color/windowBackground</item>
        <item name="textcolor">@color/textColorPrimary</item>
        <item name="hintcolor">@color/textColorHint</item>
        <item name="tintcolor">@color/textColorPrimary</item>
        <item name="buttoncolor">@color/buttonColor</item>
        <item name="navigationbarcolor">@color/navigationBarColor</item>
        <item name="headercolor">@color/colorNature</item>
        <item name="primarycolor">@color/colorNature</item>
    </style>

    <!-- Nature Dark application theme. -->
    <style name="NatureDarkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textColorPrimaryDarkmode</item>
        <item name="android:buttonBarStyle">@color/textColorPrimaryDarkmode</item>
        <item name="colorPrimary">@color/colorNature</item>
        <item name="colorPrimaryDark">@color/colorNatureHeader</item>
        <item name="colorAccent">@color/colorNature</item>
        <item name="backgroundcolor">@color/windowBackgroundDarkmode</item>
        <item name="textcolor">@color/textColorPrimaryDarkmode</item>
        <item name="hintcolor">@color/textColorHintDarkmode</item>
        <item name="tintcolor">@color/textColorPrimaryDarkmode</item>
        <item name="buttoncolor">@color/buttonColorDarkmode</item>
        <item name="navigationbarcolor">@color/navigationBarColorDarkmode</item>
        <item name="headercolor">@color/colorNatureDark</item>
        <item name="primarycolor">@color/colorNature</item>
    </style>

    <!-- Fire application theme. -->
    <style name="FireTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="colorPrimary">@color/colorFire</item>
        <item name="colorPrimaryDark">@color/colorFireHeader</item>
        <item name="colorAccent">@color/colorFire</item>
        <item name="backgroundcolor">@color/windowBackground</item>
        <item name="textcolor">@color/textColorPrimary</item>
        <item name="hintcolor">@color/textColorHint</item>
        <item name="tintcolor">@color/textColorPrimary</item>
        <item name="buttoncolor">@color/buttonColor</item>
        <item name="navigationbarcolor">@color/navigationBarColor</item>
        <item name="headercolor">@color/colorFire</item>
        <item name="primarycolor">@color/colorFire</item>
    </style>

    <!-- Fire Dark application theme. -->
    <style name="FireDarkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/textColorPrimaryDarkmode</item>
        <item name="android:buttonBarStyle">@color/textColorPrimaryDarkmode</item>
        <item name="colorPrimary">@color/colorFire</item>
        <item name="colorPrimaryDark">@color/colorFireHeader</item>
        <item name="colorAccent">@color/colorFire</item>
        <item name="backgroundcolor">@color/windowBackgroundDarkmode</item>
        <item name="textcolor">@color/textColorPrimaryDarkmode</item>
        <item name="hintcolor">@color/textColorHintDarkmode</item>
        <item name="tintcolor">@color/textColorPrimaryDarkmode</item>
        <item name="buttoncolor">@color/buttonColorDarkmode</item>
        <item name="navigationbarcolor">@color/navigationBarColorDarkmode</item>
        <item name="headercolor">@color/colorFireDark</item>
        <item name="primarycolor">@color/colorFire</item>
    </style>

</resources>
